import { contextBridge, ipcRenderer } from "electron";
contextBridge.exposeInMainWorld("usage", {hasKey:()=>ipcRenderer.invoke("has-key"),saveKey:key=>ipcRenderer.invoke("save-key",key),removeKey:()=>ipcRenderer.invoke("remove-key"),saveCsv:csv=>ipcRenderer.invoke("save-csv",csv),notifyThreshold:(tokens,threshold)=>ipcRenderer.invoke("notify-threshold",tokens,threshold),onTrayRefresh:(callback)=>ipcRenderer.on("tray-refresh",callback),getReport:(days)=>ipcRenderer.invoke("get-report",days)});
